name: LinkedIn post automation

on: [push]
  # schedule:
    # - cron: "0 0 1 * *"
      
jobs:
  logLatestRelease:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout repo
       uses: actions/checkout@v2
       
     - name: Setup python
       uses: actions/setup-python@v2
       with:
          python-version: 3.8

       
     - name : Run LinkedIn script
       run: |
          pip install -r requirements.txt
          python main.py  ${{secrets.SEND_TO}} ${{secrets.SEND_FROM}} ${{secrets.MAIL_PASSWORD}} ${{secrets.API}} ${{secrets.LINKEDIN_ACCESS_TOKEN}} ${{secrets.LINKEDIN_ID_URN}}
#username: ${{secrets.MAIL_USERNAME}}